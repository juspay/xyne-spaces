// Re-export from the shared package so there is a single source of truth.
// The implementation lives in shared/src/utils/slaCalculator.ts.
export { addSlaHours, computeSlaDueDates } from '@xyne/shared';
