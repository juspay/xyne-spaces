# TEMPORARILY DISABLED (open-source).
# The commit-msg commitlint check enforces a mandatory ticket ID via
# commitlint.config.js (`ticket-required` rule). External / community
# contributors cannot obtain a ticket ID yet, so this would block them from
# committing at all. Disabled until a contributor-friendly commit convention
# (Conventional Commits without the ticket requirement) is in place.
# See docs/github-actions-ci-plan.md ("Convention gate dropped").
#
# To restore, uncomment the block below:
#
# BRANCH_NAME=$(git branch | grep '*' | sed 's/* //')
#
# if [[ $BRANCH_NAME != *"no branch"* ]]
# then
#   npx --no -- commitlint --edit "${1}"
# fi

exit 0
