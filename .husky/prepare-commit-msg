#!/usr/bin/env sh

COMMIT_MSG_FILE=$1
JUSTIFICATIONS_FILE=".git/SERVICE_JUSTIFICATIONS"

if [ -f "$JUSTIFICATIONS_FILE" ]; then
    cat "$JUSTIFICATIONS_FILE" >> "$COMMIT_MSG_FILE"
    rm "$JUSTIFICATIONS_FILE"
fi
