# Generated by `pnpm build:prototypes`. Prettier expands it from one line to ~3900
# lines of float arrays, which then fails format:check on every rebuild and buries
# real changes in review. It is a build artifact, not source.
src/services/onDeviceIntent/prototypes.json
