export function shouldLoadSdlcWikiPages(section: string | undefined): boolean {
  return section === 'wiki';
}

export function shouldLoadSdlcWikiRun(section: string | undefined): boolean {
  return section === 'wiki';
}
