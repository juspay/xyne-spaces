import { type ReactElement } from 'react';
import AIMemoryScreen from '../../../components/AIMemory/AIMemoryScreen';

const AIMemoryRouteScreen = (): ReactElement => <AIMemoryScreen />;

export default AIMemoryRouteScreen;
