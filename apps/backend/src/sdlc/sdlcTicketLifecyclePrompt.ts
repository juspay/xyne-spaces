export function buildSdlcTicketLifecycleInstruction(ticketReference?: string): string {
  const reference = ticketReference ? ` for ticket ${ticketReference}` : '';
  return `For explicit implementation work${reference}, manage its board lifecycle throughout the work, not only when work starts. Before the first transition, call spaces-tickets to resolve the ticket's Internal ID and current board/stage, then call spaces-boards to read the board's valid stages. After each milestone is actually verified—such as when implementation begins, when a commit succeeds, and when a pull request is verified—call spaces-update-ticket with the Internal ID and the exact existing stage name when the board has a semantically matching next stage. For example, if the board has a separate Commit stage, move there only after the commit succeeds. Never mark a test-success stage when checks failed. Do not invent stage names, skip forward before a milestone, move backward, or repeat an already-pending transition. If no matching stage exists, leave the ticket unchanged and report that. If a transition is queued for approval, report it as pending, do not claim the ticket moved, and do not retry it. A missing, rejected, failed, or unavailable ticket-stage transition must be reported but must not block the remaining implementation, commit, push, or draft pull request.`;
}

export function buildSdlcWorkDeliveryInstruction(): string {
  return `Preserve usable implementation work even when repository verification does not pass. After editing, review git diff and git status once for requested scope, incomplete edits, unresolved merge conflicts, and suspected secrets. Run each relevant existing check once and record every attempted command as passed, failed, unavailable, or timed out. If a check cannot start because its package manager or dependency is missing, attempt the repository-documented bootstrap or compatible package-manager fallback once; if it still cannot run, record it as unavailable. Do not loop on checks or discard usable changes because a check failed. Check failures are non-blocking: after the single review and check attempts, commit and push the usable work, then create and verify the draft pull request. Put a prominent Verification warning in both the draft pull request body and final response, listing each command, outcome, and concise failure or timeout detail. Never claim a failed, unavailable, or timed-out check passed. Stop before delivery only when there are no usable requested changes, the review finds unresolved merge conflicts or suspected secrets, the branch is unsafe or is the default branch, or commit, push, or pull-request creation or verification itself fails.`;
}
