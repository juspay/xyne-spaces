use flake
# Load local secrets into the dev shell so env-driven configs resolve
# (project.nix `builtins.getEnv`, scripts/sandbox.sh `${...}`, compose interpolation).
dotenv_if_exists backend/.env.local
