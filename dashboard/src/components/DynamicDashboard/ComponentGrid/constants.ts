// Grid geometry shared with the backend's AI tile placement.
export { GRID_COLS, DEFAULT_TILE_W, DEFAULT_TILE_H } from '@xyne/shared';
// Render-only constants (frontend layout, not placement math).
export const ROW_HEIGHT_PX = 96;
export const TILE_MARGIN_PX: [number, number] = [24, 24];
export const MIN_TILE_W = 2;
export const MIN_TILE_H = 2;
