import type { ReactElement } from 'react';

export const XyneSpacesIcon = (): ReactElement => {
  return (
    <span
      className='flex items-center justify-center w-4 h-4 rounded-[3px] bg-xyne-red-500 text-white text-[10px] font-bold leading-none'
      aria-hidden
    >
      X
    </span>
  );
};
