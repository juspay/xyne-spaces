// Fallback logic is now in shared useQuery. This file kept for any existing imports.
// useQuery from @xyne/shared/hooks already includes fallback support via ZeroFallbackProvider.
export { useQuery as useQueryWithFallback } from '@xyne/shared/hooks';
